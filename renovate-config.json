{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Shared Renovate preset for SP Night port repositories. Engine bumps are NOT handled here — the engine's release fan-out pins each mapping and regenerates it in the same pull request, because a pin bumped without regenerating cannot pass the port's own CI. This preset covers the ordinary infrastructure: GitHub Actions.",
  "extends": [
    "config:recommended",
    ":semanticCommits",
    ":semanticCommitTypeAll(chore)"
  ],
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "GitHub Actions are infrastructure, not the theme. Group and automerge the minors.",
      "matchManagers": ["github-actions"],
      "groupName": "github actions",
      "semanticCommitType": "ci",
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    }
  ]
}
